apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kcd-uifullname" . }}-test-connection"
  labels:
    {{- include "kcd-uilabels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kcd-uifullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
